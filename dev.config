[{db_handler,
  [{connections,
    [{default, pg,
      [{name, default}, % this is pool name. if not defined - use first pool
       {host, "localhost"},
       {user, "postgres"},
       {password, undefined},
       {database, undefined}
      ],
      []}]}
  ]
 }
].
